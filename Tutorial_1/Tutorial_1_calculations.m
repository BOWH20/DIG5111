% Part (a)
A = 3+4/5+6;
fprintf('Value of A for part (a): %f\n', A);

% Part (b) 
A = 2*pi^2;
fprintf('Value of A for part (b): %f\n', A);

% Part (c)
A = sqrt(2);
fprintf('Value of A for part (c): %f\n', A);

% Part (d) 
A = (0.0000123 + 5.67e-3) * 0.4567e-4;
fprintf('Value of A for part (d): %e\n', A);